name: cardily
description: "Cardily"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0-277.0.dev <4.0.0'

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true